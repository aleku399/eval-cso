import * as React from "react";
import UpdateClaimTypes from "../../containers/UpdateClaimTypes";
import Layout from "../Layout";

export default function EditServiceTemplate() {
  return (
    <Layout>
      <UpdateClaimTypes />
    </Layout>
  );
}
