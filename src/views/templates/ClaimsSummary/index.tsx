import * as React from "react";
import ClaimsSummary from "../../containers/ClaimsSummary";
import Layout from "../Layout";

export default function EditServiceTemplate() {
  return (
    <Layout>
      <ClaimsSummary />
    </Layout>
  );
}
