import * as React from "react";
import NpsSummary from "../../containers/NpsSummary";
import Layout from "../Layout";

export default function EditServiceTemplate() {
  return (
    <Layout>
      <NpsSummary />
    </Layout>
  );
}
