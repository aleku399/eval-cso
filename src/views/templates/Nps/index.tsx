import * as React from "react";
import CreateNps from "../../containers/CreateNps";
import Layout from "../Layout";

export default function EditServiceTemplate() {
  return (
    <Layout>
      <CreateNps />
    </Layout>
  );
}
